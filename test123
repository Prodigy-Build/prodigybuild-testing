```python
def is_prime(num):
   if num > 1:
      for i in range(2,num):
         if (num % i) == 0:
            return False
            break
      else:
         return True
   else:
      return False

def prime_sum(num1, num2):
   if is_prime(num1) and is_prime(num2):
      return num1 + num2
   else:
      return "Both numbers must be prime"

with open("test123", "w") as file:
   result = prime_sum(3, 7) # You can change these numbers
   file.write(str(result))
```